<UserControl x:Class="Finly.Views.Controls.ReportsPageControls.LoansTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="Transparent">

    <Border Style="{StaticResource Card}" Padding="12">
        <StackPanel>
            <TextBlock Text="Kredyty – podsumowanie"
                       Style="{StaticResource CardHeader}"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <lvc:CartesianChart Height="320"
                                Style="{StaticResource BaseCartesianChart}"
                                Series="{Binding LoansSeries}"
                                XAxes="{Binding LoansAxesX}"
                                YAxes="{Binding LoansAxesY}"/>
        </StackPanel>
    </Border>
</UserControl>

