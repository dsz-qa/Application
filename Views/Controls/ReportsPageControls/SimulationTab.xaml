<UserControl x:Class="Finly.Views.Controls.ReportsPageControls.SimulationTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="Transparent">

    <Border Style="{StaticResource Card}" Padding="12">
        <StackPanel>
            <TextBlock Text="Symulacja – saldo"
                       Style="{StaticResource CardHeader}"
                       FontSize="16"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

            <lvc:CartesianChart Height="320"
                                Style="{StaticResource BaseCartesianChart}"
                                Series="{Binding SimulationSeries}"
                                XAxes="{Binding SimulationAxesX}"
                                YAxes="{Binding SimulationAxesY}"/>
        </StackPanel>
    </Border>
</UserControl>

