<Window x:Class="Finly.Views.Dialogs.AddEditInvestmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Inwestycja"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="{DynamicResource App.Background}"
        Foreground="{DynamicResource App.Foreground}"
        Icon="/Assets/Icons/logoFinly.ico">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40"
                            ResizeBorderThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TITLE BAR -->
        <Grid Grid.Row="0"
              Background="{DynamicResource TitleBar.Background}"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Inwestycja"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       Foreground="{DynamicResource App.Foreground}"
                       FontSize="13"
                       shell:WindowChrome.IsHitTestVisibleInChrome="True"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Width="46" Height="32" Padding="0"
                        Background="Transparent"
                        BorderBrush="{x:Null}"
                        Foreground="{DynamicResource App.Foreground}"
                        Cursor="Hand"
                        FocusVisualStyle="{x:Null}"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"
                        Click="Close_Click">
                    <TextBlock Text="✕" FontSize="12"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- BODY -->
        <Border Grid.Row="1"
                Margin="14"
                Padding="16"
                Background="{DynamicResource App.Background}"
                BorderBrush="{DynamicResource Surface.Border}"
                BorderThickness="1"
                CornerRadius="12"
                MinWidth="560">

            <Border Padding="16"
                    Background="{DynamicResource Surface.Background}"
                    BorderBrush="{DynamicResource Surface.Border}"
                    BorderThickness="1"
                    CornerRadius="10">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,6">
                        <StackPanel>
                            <TextBlock x:Name="HeaderTitleText"
                                       Text="Dodawanie inwestycji"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource App.Foreground}"/>
                            <TextBlock Text="Dodaj inwestycję do portfela. Wyceny dodasz później jako wpisy w czasie."
                                       Opacity="0.75"
                                       Foreground="{DynamicResource App.Foreground}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Nazwa -->
                    <TextBlock Grid.Row="2" Grid.Column="0"
                               Text="Nazwa:"
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>

                    <StackPanel Grid.Row="2" Grid.Column="1">
                        <TextBox x:Name="NameTextBox"
                                 MinWidth="300"
                                 Text="{Binding Name,
                                                Mode=TwoWay,
                                                UpdateSourceTrigger=PropertyChanged,
                                                TargetNullValue='',
                                                FallbackValue=''}"
                                 TextChanged="NameTextBox_TextChanged"/>

                        <TextBlock x:Name="InlineErrorText"
                                   Text=""
                                   Foreground="Tomato"
                                   FontSize="12"
                                   Margin="2,4,0,0"
                                   Visibility="Collapsed"
                                   TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Typ -->
                    <TextBlock Grid.Row="3" Grid.Column="0"
                               Text="Typ:"
                               VerticalAlignment="Center"
                               Margin="0,8,10,0"/>

                    <ComboBox x:Name="TypeCombo"
                              Grid.Row="3" Grid.Column="1"
                              Margin="0,8,0,0"
                              SelectedValuePath="Tag"
                              SelectedValue="{Binding Type,
                                                     Mode=TwoWay,
                                                     UpdateSourceTrigger=PropertyChanged,
                                                     TargetNullValue=Other,
                                                     FallbackValue=Other}"
                              SelectionChanged="TypeCombo_SelectionChanged">

                        <!-- awaryjny default, jeśli binding nie nadpisze -->
                        <ComboBoxItem Content="Inne" Tag="Other" IsSelected="True"/>
                        <ComboBoxItem Content="Akcje" Tag="Stock"/>
                        <ComboBoxItem Content="Obligacje" Tag="Bond"/>
                        <ComboBoxItem Content="ETF" Tag="Etf"/>
                        <ComboBoxItem Content="Fundusz" Tag="Fund"/>
                        <ComboBoxItem Content="Kryptowaluty" Tag="Crypto"/>
                        <ComboBoxItem Content="Lokata" Tag="Deposit"/>
                        <ComboBoxItem Content="Złoto" Tag="Gold"/>
                    </ComboBox>

                    <!-- Opis -->
                    <TextBlock Grid.Row="4" Grid.Column="0"
                               Text="Opis:"
                               VerticalAlignment="Top"
                               Margin="0,10,10,0"/>

                    <TextBox x:Name="DescriptionTextBox"
                             Grid.Row="4" Grid.Column="1"
                             Margin="0,10,0,0"
                             Height="80"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Text="{Binding Description,
                                            Mode=TwoWay,
                                            UpdateSourceTrigger=PropertyChanged,
                                            TargetNullValue='',
                                            FallbackValue=''}"/>

                    <!-- Buttons -->
                    <StackPanel Grid.Row="6"
                                Grid.ColumnSpan="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="Anuluj"
                                Style="{StaticResource SecondaryButton}"
                                IsCancel="True"
                                Click="Cancel_Click"/>

                        <Button Content="Zapisz"
                                Style="{StaticResource PrimaryButton}"
                                Margin="8,0,0,0"
                                IsDefault="True"
                                Click="Ok_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>

