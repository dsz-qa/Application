<UserControl x:Class="Finly.Pages.ReportsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Finly.Views.Controls"
             xmlns:vm="clr-namespace:Finly.ViewModels"
             xmlns:conv="clr-namespace:Finly.Helpers.Converters"
             Foreground="{StaticResource App.Foreground}">

    <UserControl.DataContext>
        <vm:ReportsViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />

        <!-- system brushes override for dark popups -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#26282D" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#EAEAEA" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#26282D" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#EAEAEA" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3B82F6" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF" />

        <!-- Basic dark styles -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="Padding" Value="6,4" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="Padding" Value="4" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="Padding" Value="6,2" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource Accent}" />
                    <Setter Property="Foreground" Value="{StaticResource OnAccent}" />
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource Sidebar.Hover}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4,4,0,0"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface.Background}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}" />
                                <Setter Property="Panel.ZIndex" Value="1" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Sidebar.Hover}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#777777" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Headers -->
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0,0,0,1"
                                    Padding="6">
                                <TabPanel IsItemsHost="True"
                                          Background="{TemplateBinding Background}"
                                          KeyboardNavigation.TabIndex="0" />
                            </Border>

                            <!-- Content -->
                            <Border Grid.Row="1"
                                    Background="{StaticResource Surface.Background}"
                                    BorderBrush="{StaticResource Surface.Border}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,8,0,0">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="RowBackground" Value="{x:Null}" />
            <Setter Property="AlternatingRowBackground" Value="{StaticResource Control.Background}" />
            <Setter Property="GridLinesVisibility" Value="None" />
        </Style>
    </UserControl.Resources>

    <!-- Main layout -->
    <Grid Margin="12,0,12,12" Background="{StaticResource App.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="320" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Filtry -->
        <Border Grid.Row="0"
                Grid.Column="0"
                Background="{StaticResource Surface.Background}"
                Padding="12"
                CornerRadius="6"
                Margin="0,0,12,0"
                BorderBrush="{StaticResource Surface.Border}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Filtry" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" />

                <TextBlock Text="Źródło" FontWeight="Bold" />
                <ComboBox ItemsSource="{Binding SourceOptions}"
                          SelectedItem="{Binding SelectedSource, Mode=TwoWay}"
                          Margin="0,4,0,8">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <!-- Na razie pokazujemy nazwy enuma, np. All, FreeCash itd.
                                 Jak będziesz chciała, zrobimy ładne polskie etykiety przez konwerter. -->
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Konto bankowe -->
                <StackPanel Orientation="Vertical"
                            Margin="0,4,0,8"
                            Visibility="{Binding ShowBankSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="Konto" FontWeight="Bold" />
                    <ComboBox ItemsSource="{Binding BankAccounts}"
                              SelectedItem="{Binding SelectedBankAccount}"
                              DisplayMemberPath="AccountName"
                              SelectedValuePath="Id" />
                </StackPanel>

                <!-- Koperta -->
                <StackPanel Orientation="Vertical"
                            Margin="0,4,0,8"
                            Visibility="{Binding ShowEnvelopeSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="Koperta" FontWeight="Bold" />
                    <ComboBox ItemsSource="{Binding Envelopes}"
                              SelectedItem="{Binding SelectedEnvelope}" />
                </StackPanel>

                <TextBlock Text="Kategoria" FontWeight="Bold" />
                <ComboBox ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          Margin="0,4,0,8" />

                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="Odśwież" Command="{Binding RefreshCommand}" Width="100" />
                    <Button Content="Eksport" Width="100" Margin="8,0,0,0">
                        <Button.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="CSV" Command="{Binding ExportCsvCommand}" />
                                <MenuItem Header="Excel" Command="{Binding ExportExcelCommand}" />
                                <MenuItem Header="PDF" Command="{Binding ExportPdfCommand}" />
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- CENTER: pasek okresu + zakładki + wykres + tabele -->
        <Border Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="1"
                Background="{StaticResource Surface.Background}"
                Padding="12"
                CornerRadius="6"
                Margin="0,0,12,0"
                BorderBrush="{StaticResource Surface.Border}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Pasek okresu -->
                <ctrls:PeriodBarControl x:Name="PeriodBar"
                                        Grid.Row="0"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,12"
                                        StartDate="{Binding FromDate, Mode=TwoWay}"
                                        EndDate="{Binding ToDate, Mode=TwoWay}" />

                <!-- Zakładki (na razie same nagłówki, logika później) -->
                <TabControl Grid.Row="1">
                    <TabItem Header="Przegląd" />
                    <TabItem Header="Kategorie" />
                    <TabItem Header="Czas" />
                    <TabItem Header="Cashflow" />
                    <TabItem Header="Subskrypcje" />
                    <TabItem Header="Budżety" />
                    <TabItem Header="Cele" />
                </TabControl>

                <!-- Wykres + szczegóły / drilldown -->
                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Donut chart -->
                    <ctrls:DonutChartControl x:Name="MainChart"
                                             Grid.Column="0"
                                             HorizontalAlignment="Stretch"
                                             VerticalAlignment="Stretch"
                                             Background="Transparent" />

                    <!-- Szczegóły / drilldown -->
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Szczegóły kategorii"
                                   FontWeight="Bold"
                                   Margin="0,0,0,8" />

                        <!-- Podsumowanie kategorii -->
                        <DataGrid x:Name="SummaryGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Height="220"
                                  ItemsSource="{Binding Details}"
                                  Visibility="{Binding IsSummaryActive, Converter={StaticResource BoolToVisibilityConverter}}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Kategoria" Binding="{Binding Name}" />
                                <DataGridTextColumn Header="Kwota" Binding="{Binding Amount, StringFormat=N2}" />
                                <DataGridTextColumn Header="Udział %" Binding="{Binding SharePercent, StringFormat=N1}" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Button x:Name="BackButton"
                                Content="Wróć"
                                Command="{Binding BackCommand}"
                                Visibility="{Binding IsDrilldownActive, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="0,8,0,0" />

                        <TextBlock Text="Kliknij element wykresu, aby zobaczyć transakcje w tej kategorii."
                                   FontStyle="Italic"
                                   Margin="0,8,0,0" />

                        <!-- Lista transakcji dla wybranej kategorii -->
                        <DataGrid x:Name="DrilldownGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Height="220"
                                  ItemsSource="{Binding FilteredTransactions}"
                                  Margin="0,8,0,0"
                                  Visibility="{Binding IsDrilldownActive, Converter={StaticResource BoolToVisibilityConverter}}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Data"
                                                    Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" />
                                <DataGridTextColumn Header="Kategoria"
                                                    Binding="{Binding Category}" />
                                <DataGridTextColumn Header="Opis"
                                                    Binding="{Binding Description}" />
                                <DataGridTextColumn Header="Kwota"
                                                    Binding="{Binding Amount, StringFormat=N2}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- RIGHT: Analizy + KPI -->
        <Border Grid.Row="0"
                Grid.Column="2"
                Background="{StaticResource Surface.Background}"
                Padding="12"
                CornerRadius="6"
                BorderBrush="{StaticResource Surface.Border}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Analizy finansowe"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8" />

                <!-- Sumy bieżącego okresu -->
                <Border Background="{StaticResource Control.Background}"
                        CornerRadius="4"
                        Padding="8"
                        Margin="0,0,0,8"
                        BorderBrush="{StaticResource Surface.Border}"
                        BorderThickness="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Suma wydatków:" FontWeight="SemiBold" Width="160" />
                            <TextBlock Text="{Binding ExpensesTotalStr}" HorizontalAlignment="Right" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Suma przychodów:" FontWeight="SemiBold" Width="160" />
                            <TextBlock Text="{Binding IncomesTotalStr}" HorizontalAlignment="Right" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Saldo:" FontWeight="SemiBold" Width="160" />
                            <TextBlock Text="{Binding BalanceTotalStr}" HorizontalAlignment="Right" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Info o porównywanym okresie -->
                <TextBlock Text="{Binding ComparisonPeriodLabel}"
                           FontStyle="Italic"
                           Margin="0,0,0,8" />

                <!-- Insight cards -->
                <ItemsControl ItemsSource="{Binding Insights}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource Control.Background}"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,0,0,8"
                                    BorderBrush="{StaticResource Surface.Border}"
                                    BorderThickness="1">
                                <TextBlock Text="{Binding}"
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource Control.Foreground}" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="KPI"
                           FontWeight="Bold"
                           Margin="0,12,0,6" />

                <ItemsControl ItemsSource="{Binding KPIList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Stretch"
                                        Margin="0,2">
                                <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Width="190" />
                                <TextBlock Text="{Binding Value}" HorizontalAlignment="Right" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
