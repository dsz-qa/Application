<UserControl x:Class="Finly.Pages.ReportsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:Finly.Views.Controls"
             xmlns:vm="clr-namespace:Finly.ViewModels"
             xmlns:conv="clr-namespace:Finly.Helpers.Converters"
             Foreground="{StaticResource App.Foreground}">

    <UserControl.DataContext>
        <vm:ReportsViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <conv:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />

        <!-- Ikona kalendarza (fallback wektorowy) -->
        <DrawingImage x:Key="CalendarIcon">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="{StaticResource Control.Foreground}" Geometry="M1,2 L17,2 L17,5 L1,5 Z" />
                    <GeometryDrawing Brush="{x:Null}">
                        <GeometryDrawing.Pen>
                            <Pen Brush="{StaticResource Surface.Border}" Thickness="1" />
                        </GeometryDrawing.Pen>
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="1,4,16,14" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="{StaticResource Control.Foreground}">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry Center="4,2" RadiusX="1" RadiusY="1" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="{StaticResource Control.Foreground}">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry Center="14,2" RadiusX="1" RadiusY="1" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="{StaticResource Control.Foreground}">
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <RectangleGeometry Rect="3,7,1,1" />
                                <RectangleGeometry Rect="6,7,1,1" />
                                <RectangleGeometry Rect="9,7,1,1" />
                                <RectangleGeometry Rect="12,7,1,1" />
                                <RectangleGeometry Rect="15,7,1,1" />
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>

        <!-- Ciemne brush’e dla popupów/comboboxów -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#26282D" />
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#EAEAEA" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#26282D" />
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#EAEAEA" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3B82F6" />
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFFFFF" />

        <!-- Podstawowe style ciemne -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="Padding" Value="6,4" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="Padding" Value="4" />
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
        </Style>

        <!-- TabControl / TabItem -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4,4,0,0"
                                Padding="{TemplateBinding Padding}"
                                Margin="{TemplateBinding Margin}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Surface.Background}" />
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}" />
                                <Setter Property="Panel.ZIndex" Value="1" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Sidebar.Hover}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#777777" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="BorderBrush" Value="{StaticResource Surface.Border}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Headers -->
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0,0,0,1"
                                    Padding="6">
                                <TabPanel IsItemsHost="True"
                                          Background="{TemplateBinding Background}"
                                          KeyboardNavigation.TabIndex="0" />
                            </Border>

                            <!-- Content -->
                            <Border Grid.Row="1"
                                    Background="{StaticResource Surface.Background}"
                                    BorderBrush="{StaticResource Surface.Border}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,8,0,0">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{StaticResource Control.Background}" />
            <Setter Property="Foreground" Value="{StaticResource Control.Foreground}" />
            <Setter Property="RowBackground" Value="{x:Null}" />
            <Setter Property="AlternatingRowBackground" Value="{StaticResource Control.Background}" />
            <Setter Property="GridLinesVisibility" Value="None" />
        </Style>
    </UserControl.Resources>

    <!-- GŁÓWNY UKŁAD: środek + prawa kolumna KPI -->
    <Grid Margin="12,0,12,12" Background="{StaticResource App.Background}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <!-- środkowy panel (pasek + zakładki + wykres) -->
            <ColumnDefinition Width="360" />
            <!-- Analizy finansowe / KPI, trochę szersze -->
        </Grid.ColumnDefinitions>

        <!-- ŚRODKOWY PANEL: okres, zakładki, filtry, wykres, szczegóły -->
        <Border Grid.Column="0"
                Background="{StaticResource Surface.Background}"
                Padding="12"
                CornerRadius="6"
                Margin="0,0,12,0"
                BorderBrush="{StaticResource Surface.Border}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!-- Pasek okresu -->
                    <RowDefinition Height="Auto" />
                    <!-- Zakładki + filtry -->
                    <RowDefinition Height="*" />
                    <!-- Wykres + szczegóły -->
                </Grid.RowDefinitions>

                <!-- Pasek wyboru okresu -->
                <ctrls:PeriodBarControl x:Name="PeriodBar"
                                        Grid.Row="0"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,12"
                                        StartDate="{Binding FromDate, Mode=TwoWay}"
                                        EndDate="{Binding ToDate, Mode=TwoWay}" />

                <!-- Zakładki + filtry -->
                <StackPanel Grid.Row="1">
                    <!-- Zakładki -->
                    <TabControl Background="{StaticResource Control.Background}"
                                Foreground="{StaticResource Control.Foreground}">
                        <TabItem Header="Przegląd" />
                        <TabItem Header="Kategorie" />
                        <TabItem Header="Czas" />
                        <TabItem Header="Cashflow" />
                        <TabItem Header="Subskrypcje" />
                        <TabItem Header="Budżety" />
                        <TabItem Header="Cele" />
                    </TabControl>

                    <!-- Wiersz z filtrami: Źródło / Konto / Koperta / Kategoria / przyciski -->
                    <StackPanel Orientation="Horizontal"
                                Margin="0,8,0,0"
                                VerticalAlignment="Center">
                        <!-- Źródło -->
                        <TextBlock Text="Źródło"
                                   FontWeight="Bold"
                                   Margin="0,0,6,0"
                                   VerticalAlignment="Center" />
                        <ComboBox ItemsSource="{Binding SourceOptions}"
                                  SelectedItem="{Binding SelectedSource, Mode=TwoWay}"
                                  Width="140"
                                  Margin="0,0,16,0" />

                        <!-- Konto bankowe (widoczne tylko przy kontach bankowych) -->
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,0,16,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding ShowBankSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Konto"
                                       FontWeight="Bold"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center" />
                            <ComboBox ItemsSource="{Binding BankAccounts}"
                                      SelectedItem="{Binding SelectedBankAccount}"
                                      DisplayMemberPath="AccountName"
                                      SelectedValuePath="Id"
                                      Width="180" />
                        </StackPanel>

                        <!-- Koperta (widoczna tylko przy źródle: Koperty) -->
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,0,16,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding ShowEnvelopeSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Koperta"
                                       FontWeight="Bold"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center" />
                            <ComboBox ItemsSource="{Binding Envelopes}"
                                      SelectedItem="{Binding SelectedEnvelope}"
                                      Width="170" />
                        </StackPanel>

                        <!-- Kategoria -->
                        <TextBlock Text="Kategoria"
                                   FontWeight="Bold"
                                   Margin="0,0,6,0"
                                   VerticalAlignment="Center" />
                        <ComboBox ItemsSource="{Binding Categories}"
                                  SelectedItem="{Binding SelectedCategory}"
                                  Width="180"
                                  Margin="0,0,16,0" />

                        <!-- Przyciski -->
                        <Button Content="Odśwież"
                                Command="{Binding RefreshCommand}"
                                Width="100" />
                        <Button Content="Eksport"
                                Width="100"
                                Margin="8,0,0,0">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="CSV"   Command="{Binding ExportCsvCommand}" />
                                    <MenuItem Header="Excel" Command="{Binding ExportExcelCommand}" />
                                    <MenuItem Header="PDF"   Command="{Binding ExportPdfCommand}" />
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- Wykres + szczegóły / drilldown -->
                <Grid Grid.Row="2" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Wykres kołowy / donut -->
                    <ctrls:DonutChartControl x:Name="MainChart"
                                             Grid.Column="0"
                                             HorizontalAlignment="Stretch"
                                             VerticalAlignment="Stretch"
                                             Background="Transparent" />

                    <!-- Szczegóły po prawej od wykresu -->
                    <StackPanel Grid.Column="1" Margin="12,0,0,0">
                        <TextBlock Text="Szczegóły kategorii"
                                   FontWeight="Bold"
                                   Margin="0,0,0,8" />

                        <!-- Podsumowanie kategorii -->
                        <DataGrid x:Name="SummaryGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Height="220"
                                  Background="{StaticResource Control.Background}"
                                  Foreground="{StaticResource Control.Foreground}"
                                  RowBackground="{x:Null}"
                                  ItemsSource="{Binding Details}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Kategoria" Binding="{Binding Name}" />
                                <DataGridTextColumn Header="Kwota" Binding="{Binding Amount, StringFormat=N2}" />
                                <DataGridTextColumn Header="Udział %" Binding="{Binding SharePercent, StringFormat=N1}" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Button x:Name="BackButton"
                                Content="Wróć"
                                Margin="0,8,0,0"
                                Command="{Binding BackCommand}"
                                Visibility="{Binding IsDrilldownActive, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <TextBlock Text="Kliknij element wykresu, aby zobaczyć szczegóły transakcji."
                                   FontStyle="Italic"
                                   Margin="0,8,0,0" />

                        <!-- Lista transakcji w drilldown -->
                        <DataGrid x:Name="DrilldownGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Height="220"
                                  Background="{StaticResource Control.Background}"
                                  Foreground="{StaticResource Control.Foreground}"
                                  RowBackground="{x:Null}"
                                  Margin="0,8,0,0"
                                  ItemsSource="{Binding FilteredTransactions}"
                                  Visibility="{Binding IsDrilldownActive, Converter={StaticResource BoolToVisibilityConverter}}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Data" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" />
                                <DataGridTextColumn Header="Kategoria" Binding="{Binding Category}" />
                                <DataGridTextColumn Header="Opis" Binding="{Binding Description}" />
                                <DataGridTextColumn Header="Kwota" Binding="{Binding Amount, StringFormat=N2}" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- PRAWY PANEL: Analizy finansowe / KPI -->
        <Border Grid.Column="1"
                Background="{StaticResource Surface.Background}"
                Padding="12"
                CornerRadius="6"
                BorderBrush="{StaticResource Surface.Border}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Analizy finansowe"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8" />

                <!-- Suma wydatków / przychodów / saldo -->
                <Border Background="{StaticResource Control.Background}"
                        CornerRadius="4"
                        Padding="8"
                        Margin="0,0,0,8"
                        BorderBrush="{StaticResource Surface.Border}"
                        BorderThickness="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Suma wydatków:"
                                       FontWeight="SemiBold"
                                       Width="160" />
                            <TextBlock Text="{Binding ExpensesTotalStr}"
                                       HorizontalAlignment="Right" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Suma przychodów:"
                                       FontWeight="SemiBold"
                                       Width="160" />
                            <TextBlock Text="{Binding IncomesTotalStr}"
                                       HorizontalAlignment="Right" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <TextBlock Text="Saldo:"
                                       FontWeight="SemiBold"
                                       Width="160" />
                            <TextBlock Text="{Binding BalanceTotalStr}"
                                       HorizontalAlignment="Right" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Opisy okresów / insighty -->
                <ItemsControl ItemsSource="{Binding Insights}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource Control.Background}"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,0,0,8"
                                    BorderBrush="{StaticResource Surface.Border}"
                                    BorderThickness="1">
                                <TextBlock Text="{Binding}"
                                           TextWrapping="Wrap"
                                           Foreground="{StaticResource Control.Foreground}" />
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- KPI lista -->
                <TextBlock Text="KPI"
                           FontWeight="Bold"
                           Margin="0,12,0,6" />
                <ItemsControl ItemsSource="{Binding KPIList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Stretch"
                                        Margin="0,2">
                                <TextBlock Text="{Binding Key}"
                                           FontWeight="SemiBold"
                                           Width="220" />
                                <TextBlock Text="{Binding Value}"
                                           HorizontalAlignment="Right" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
