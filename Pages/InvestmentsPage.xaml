<UserControl x:Class="Finly.Pages.InvestmentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Finly.Pages"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Reuse tych samych stylów co w innych stronach -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background"      Value="{DynamicResource Surface.Background}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius"    Value="10"/>
            <Setter Property="Padding"         Value="12"/>
            <Setter Property="Margin"          Value="0,12,16,0"/>
        </Style>

        <Style x:Key="AccentCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="BorderBrush"     Value="{DynamicResource Brand.Blue}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin"          Value="0,16,16,0"/>
        </Style>

        <Style x:Key="CardHeader" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="Opacity"    Value="0.9"/>
        </Style>

        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin"     Value="0,4,0,0"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
        </Style>

        <!-- DataTemplate dla inwestycji -->
        <DataTemplate DataType="{x:Type local:InvestmentVm}">
            <Border Style="{StaticResource Card}" Width="340">
                <StackPanel>
                    <!-- Nazwa + pomarańczowa ikonka inwestycji -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6" VerticalAlignment="Center">
                        <Rectangle Width="22" Height="22" Margin="0,0,8,0" VerticalAlignment="Center" Fill="#F39C12">
                            <Rectangle.OpacityMask>
                                <ImageBrush ImageSource="pack://application:,,,/Assets/Icons/inwestycje.png"/>
                            </Rectangle.OpacityMask>
                        </Rectangle>

                        <TextBlock FontSize="16" FontWeight="Bold" Foreground="{DynamicResource App.Foreground}" VerticalAlignment="Center">
                            <Run Text="{Binding Name}"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Wartość docelowa: " FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="{Binding TargetAmount, StringFormat={}{0:N2} zł}" Foreground="{DynamicResource App.Foreground}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Aktualna wartość: " FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="{Binding CurrentAmount, StringFormat={}{0:N2} zł}" Foreground="{DynamicResource App.Foreground}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Do celu pozostało: " FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="{Binding Remaining, StringFormat={}{0:N2} zł}" Foreground="{DynamicResource App.Foreground}"/>
                    </StackPanel>

                    <!-- Termin pod "Do celu pozostało:" -->
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Termin: " FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="{Binding TargetDate, StringFormat={}{0:dd.MM.yyyy}}" Foreground="{DynamicResource App.Foreground}"/>
                    </StackPanel>

                    <!-- Pasek postępu (opcjonalnie procent) -->
                    <StackPanel Margin="0,6,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Progres: " FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}"/>
                            <TextBlock Text="{Binding CompletionPercent, StringFormat={}{0:N0}%}" Foreground="{DynamicResource App.Foreground}"/>
                        </StackPanel>
                        <ProgressBar Height="6" Margin="0,4,0,0" Minimum="0" Maximum="100" Value="{Binding CompletionPercent, Mode=OneWay}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Miesięcznie trzeba odkładać: " FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="{Binding MonthlyNeeded, StringFormat={}{0:N2} zł}" Foreground="{DynamicResource App.Foreground}"/>
                    </StackPanel>

                    <TextBlock Text="{Binding Description}" Margin="0,6,0,0" TextWrapping="Wrap" Foreground="{DynamicResource App.Foreground}"/>

                    <!-- Przyciski -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Click="EditInvestment_Click" Margin="0,0,8,0" Padding="8,4" MinWidth="80">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/Icons/edit.png" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Edytuj" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Click="DeleteInvestment_Click" Padding="8,4" MinWidth="80">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/Icons/delete.png" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Usuń" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Panel potwierdzenia usunięcia -->
                    <StackPanel x:Name="InvestmentDeleteConfirmPanel" Orientation="Vertical" Margin="0,6,0,0" Visibility="Collapsed">
                        <TextBlock Text="Czy na pewno usunąć inwestycję?" FontSize="12" Foreground="{DynamicResource App.Foreground}" Margin="0,0,0,4"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Tak" Margin="0,0,8,0" Padding="8,3" MinWidth="60" Click="DeleteInvestmentConfirm_Click"/>
                            <Button Content="Nie" Padding="8,3" MinWidth="60" Click="DeleteInvestmentCancel_Click"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- Kafelek Dodaj inwestycję -->
        <DataTemplate DataType="{x:Type local:AddInvestmentTile}">
            <Border Style="{StaticResource Card}" Width="260" Cursor="Hand" MouseLeftButtonUp="AddInvestmentCard_Click">
                <Grid>
                    <Border Margin="10" BorderThickness="1" CornerRadius="8" BorderBrush="{DynamicResource Surface.Border}"/>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,16,0,16">
                        <TextBlock Text="+" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="Dodaj inwestycję" Margin="0,6,0,0" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- KPI -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource AccentCard}">
                    <StackPanel>
                        <TextBlock Text="Inwestycje – łączna wartość docelowa" Style="{StaticResource CardHeader}" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalTargetText" Style="{StaticResource KpiValueStyle}" FontSize="24"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource AccentCard}">
                    <StackPanel>
                        <TextBlock Text="Aktualna wartość inwestycji" Style="{StaticResource CardHeader}" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalCurrentText" Style="{StaticResource KpiValueStyle}" FontSize="24"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource AccentCard}">
                    <StackPanel>
                        <TextBlock Text="Łącznie miesięcznie trzeba odkładać" Style="{StaticResource CardHeader}" FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="TotalMonthlyText" Style="{StaticResource KpiValueStyle}" FontSize="24"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Lista inwestycji + kafelek Dodaj -->
            <StackPanel Grid.Row="1" Margin="0,12,0,0">
                <Separator Height="1" Background="{DynamicResource Surface.Border}" Opacity="0.8" Margin="0,0,0,8"/>
                <TextBlock Text="Inwestycje" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource App.Foreground}" Margin="0,4,0,12"/>
                <ItemsControl x:Name="InvestmentsRepeater" Margin="0,8,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </StackPanel>

            <!-- Formularz Dodaj / Edytuj -->
            <Border x:Name="FormBorder" Grid.Row="2" Margin="0,16,0,24" Background="Transparent" BorderBrush="{DynamicResource Surface.Border}" BorderThickness="1" CornerRadius="10" Padding="16" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" MaxWidth="600">
                    <TextBlock x:Name="FormHeader" Text="Dodaj inwestycję" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>

                    <Grid Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="260"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Nazwa:" Margin="0,4,8,4" VerticalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBox x:Name="InvestmentNameBox" Grid.Row="0" Grid.Column="1"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Wartość docelowa:" Margin="0,4,8,4" VerticalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBox x:Name="InvestmentTargetBox" Grid.Row="1" Grid.Column="1" HorizontalContentAlignment="Right" Text="0,00" GotFocus="AmountBox_GotFocus" LostFocus="AmountBox_LostFocus"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Aktualnie:" Margin="0,4,8,4" VerticalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBox x:Name="InvestmentCurrentBox" Grid.Row="2" Grid.Column="1" HorizontalContentAlignment="Right" Text="0,00" GotFocus="AmountBox_GotFocus" LostFocus="AmountBox_LostFocus"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Termin (data):" Margin="0,4,8,4" VerticalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                        <DatePicker x:Name="InvestmentTargetDatePicker" Grid.Row="3" Grid.Column="1"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Opis:" Margin="0,4,8,4" VerticalAlignment="Top" Foreground="{DynamicResource App.Foreground}"/>
                        <TextBox x:Name="InvestmentDescriptionBox" Grid.Row="4" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                        <Button Content="Zapisz" Width="110" Margin="0,0,8,0" Click="AddInvestment_Click"/>
                        <Button Content="Anuluj" Width="110" Click="ClearInvestmentForm_Click"/>
                    </StackPanel>

                    <TextBlock x:Name="InvestmentFormMessage" Margin="0,8,0,0" HorizontalAlignment="Center" Foreground="OrangeRed"/>
                </StackPanel>
            </Border>

        </Grid>
    </ScrollViewer>
</UserControl>
