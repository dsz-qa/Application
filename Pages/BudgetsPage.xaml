<UserControl x:Class="Finly.Pages.BudgetsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:local="clr-namespace:Finly.Pages"
             x:Name="Root"
             Background="{DynamicResource App.Background}"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType">

    <UserControl.Resources>

        <!-- Konwertery -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:StringNullOrEmptyToVisibilityConverter x:Key="StringToVisibility"/>
        <local:ProgressToWidthConverter x:Key="ProgressToWidthConv"/>

        <!-- ===================== KARTY / TYPOGRAFIA ===================== -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface.Background}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>

        <Style x:Key="OrangeCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="BorderBrush" Value="{DynamicResource Brand.Orange}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>

        <Style x:Key="KpiCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="BorderBrush" Value="{DynamicResource Brand.Blue}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>

        <Style x:Key="CardHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
        </Style>

        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
        </Style>

        <Style x:Key="MiniLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style x:Key="MiniValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- ===================== PROGRESS BAR ===================== -->
        <Style x:Key="BudgetProgressBarStyle" TargetType="ProgressBar">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid Background="Transparent">
                            <Border CornerRadius="6"
                                    Background="{TemplateBinding Background}"/>
                            <Border x:Name="Indicator"
                                    CornerRadius="6"
                                    Background="{TemplateBinding Foreground}"
                                    HorizontalAlignment="Left">
                                <Border.Width>
                                    <MultiBinding Converter="{StaticResource ProgressToWidthConv}">
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="ActualWidth"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Value"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Maximum"/>
                                        <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Minimum"/>
                                    </MultiBinding>
                                </Border.Width>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== TABELA: NAGŁÓWEK ===================== -->
        <Style x:Key="MiniTableHeaderBorder" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface.Background}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="MiniTableHeaderText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="Opacity" Value="0.9"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="MiniTableCellText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="6,2,6,2"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>

        <Style x:Key="MiniTableCellRight" TargetType="TextBlock" BasedOn="{StaticResource MiniTableCellText}">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>

        <Style x:Key="AmountPlannedGreen" TargetType="TextBlock" BasedOn="{StaticResource MiniTableCellRight}">
            <Setter Property="Foreground" Value="LimeGreen"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="AmountSpentOrange" TargetType="TextBlock" BasedOn="{StaticResource MiniTableCellRight}">
            <Setter Property="Foreground" Value="{DynamicResource Brand.Orange}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="AmountOverRed" TargetType="TextBlock" BasedOn="{StaticResource MiniTableCellRight}">
            <Setter Property="Foreground" Value="Tomato"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="BudgetNameCell" TargetType="TextBlock" BasedOn="{StaticResource MiniTableCellText}">
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="BudgetTypeSub" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="Opacity" Value="0.85"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="6,0,6,2"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>

        <Style x:Key="BudgetListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource Brand.Orange}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== INSIGHT ===================== -->
        <Style x:Key="InsightItemBorder" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface.Background}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,6,10,0"/>
        </Style>

        <Style x:Key="InsightItemText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- ===================== TRANSAKCJE (prawa lista) ===================== -->
        <Style x:Key="TxCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface.Background}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="TxLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
        </Style>

        <Style x:Key="TxValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <DataTemplate x:Key="BudgetTransactionCardTemplate">
            <Border Style="{StaticResource TxCard}">
                <StackPanel Orientation="Vertical" Margin="4,0,0,0" Background="Transparent">

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
                        <Image Width="18" Height="18" Stretch="Uniform" Margin="0,0,8,0">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Source" Value="/Assets/Icons/wydatki.png"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsIncome}" Value="True">
                                            <Setter Property="Source" Value="/Assets/Icons/przychody.png"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsTransfer}" Value="True">
                                            <Setter Property="Source" Value="/Assets/Icons/transfer.png"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>

                        <TextBlock FontWeight="Bold" FontSize="14" Foreground="{DynamicResource App.Foreground}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Wydatek"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsIncome}" Value="True">
                                            <Setter Property="Text" Value="Przychód"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsTransfer}" Value="True">
                                            <Setter Property="Text" Value="Transfer"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                        <TextBlock Text="Data:" Style="{StaticResource TxLabel}"/>
                        <TextBlock Text="{Binding DateText, TargetNullValue=—, FallbackValue=—}" Style="{StaticResource TxValue}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                        <TextBlock Text="Kwota:" Style="{StaticResource TxLabel}"/>
                        <TextBlock Text="{Binding AmountText, TargetNullValue=—, FallbackValue=—}" Style="{StaticResource TxValue}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3"
                                Visibility="{Binding ShowFromAccount, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Z konta:" Style="{StaticResource TxLabel}"/>
                        <TextBlock Text="{Binding FromAccountName, TargetNullValue=Nie dotyczy, FallbackValue=Nie dotyczy}"
                                   Style="{StaticResource TxValue}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                        <TextBlock Text="Kategoria:" Style="{StaticResource TxLabel}"/>
                        <TextBlock Text="{Binding CategoryName, TargetNullValue=(brak), FallbackValue=(brak)}" Style="{StaticResource TxValue}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0">
                        <TextBlock Text="Opis:" Style="{StaticResource TxLabel}"/>
                        <TextBlock Text="{Binding Description, TargetNullValue=Brak danych, FallbackValue=Brak danych}"
                                   Style="{StaticResource TxValue}"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                </StackPanel>
            </Border>
        </DataTemplate>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly"
                  Background="Transparent">

        <Grid Margin="16" Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- KPI -->
            <Grid Grid.Row="0" Margin="0,0,0,12" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource KpiCard}">
                    <StackPanel Background="Transparent">
                        <TextBlock Text="Aktywne budżety (dzisiaj)" Style="{StaticResource CardHeader}"/>
                        <TextBlock x:Name="KpiActiveBudgetsText" Style="{StaticResource KpiValueStyle}" Text="0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource KpiCard}">
                    <StackPanel Background="Transparent">
                        <TextBlock Text="Planowane (aktywne)" Style="{StaticResource CardHeader}"/>
                        <TextBlock x:Name="KpiPlannedActiveText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource KpiCard}" Margin="0">
                    <StackPanel Background="Transparent">
                        <TextBlock Text="Przekroczone (aktywne)" Style="{StaticResource CardHeader}"/>
                        <TextBlock x:Name="KpiOverActiveText" Style="{StaticResource KpiValueStyle}" Text="0 | 0,00 zł"/>
                    </StackPanel>
                </Border>
            </Grid>

            <TextBlock Grid.Row="1"
                       Text="Budżety"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource App.Foreground}"
                       Margin="0,12,0,12"/>

            <Grid Grid.Row="2" Background="Transparent">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- RZĄD 1 -->
                <Grid Grid.Row="0" Margin="0,0,0,12" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*"/>
                        <ColumnDefinition Width="1.10*"/>
                        <ColumnDefinition Width="1.25*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Szczegóły budżetu -->
                    <Border Grid.Column="0"
                            Style="{StaticResource OrangeCard}"
                            Margin="0,0,10,0"
                            Height="260"
                            Padding="12">
                        <Grid VerticalAlignment="Center" Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"
                                            Margin="0,0,12,0"
                                            VerticalAlignment="Center"
                                            Background="Transparent">
                                    <TextBlock Text="Nazwa budżetu" Style="{StaticResource MiniLabel}"/>
                                    <TextBlock x:Name="BudgetNameText"
                                               Text="(wybierz budżet)"
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource App.Foreground}"
                                               TextTrimming="CharacterEllipsis"
                                               Margin="0,2,0,0"/>

                                    <TextBlock Text="Typ" Style="{StaticResource MiniLabel}" Margin="0,10,0,0"/>
                                    <TextBlock x:Name="BudgetTypeText" Style="{StaticResource MiniValue}" Text="—"/>

                                    <TextBlock Text="Okres" Style="{StaticResource MiniLabel}" Margin="0,10,0,0"/>
                                    <TextBlock x:Name="BudgetPeriodText"
                                               Style="{StaticResource MiniValue}"
                                               Text="—"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Background="Transparent">
                                    <TextBlock Text="Zaplanowano wydać" Style="{StaticResource MiniLabel}"/>
                                    <TextBlock x:Name="BudgetPlannedText"
                                               Style="{StaticResource MiniValue}"
                                               Foreground="LimeGreen"
                                               FontSize="15"
                                               Margin="0,2,0,0"/>

                                    <TextBlock Text="Wydano" Style="{StaticResource MiniLabel}" Margin="0,10,0,0"/>
                                    <TextBlock x:Name="BudgetSpentText"
                                               Style="{StaticResource MiniValue}"
                                               FontSize="14"
                                               Foreground="{DynamicResource Brand.Orange}"
                                               Margin="0,2,0,0"/>

                                    <TextBlock Text="Przekroczono" Style="{StaticResource MiniLabel}" Margin="0,10,0,0"/>
                                    <TextBlock x:Name="BudgetOverText"
                                               Style="{StaticResource MiniValue}"
                                               Foreground="Tomato"
                                               FontSize="14"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <TextBlock x:Name="BudgetMetaText" Visibility="Collapsed"/>
                            </Grid>

                            <!-- pasek wykorzystania budżetu -->
                            <Grid Grid.Row="1" Margin="0,10,0,0" Background="Transparent">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <DockPanel Grid.Row="0" LastChildFill="False" Background="Transparent">
                                    <TextBlock Text="Wykorzystanie budżetu"
                                               Foreground="{DynamicResource App.Foreground}"
                                               Opacity="0.85"
                                               FontSize="11"
                                               FontWeight="SemiBold"
                                               DockPanel.Dock="Left"/>
                                    <TextBlock x:Name="BudgetProgressText"
                                               Text="0%"
                                               Foreground="{DynamicResource App.Foreground}"
                                               FontSize="11"
                                               FontWeight="Bold"
                                               DockPanel.Dock="Right"/>
                                </DockPanel>

                                <ProgressBar Grid.Row="1"
                                             x:Name="BudgetProgressBar"
                                             Style="{StaticResource BudgetProgressBarStyle}"
                                             Margin="0,6,0,0"
                                             Foreground="LimeGreen"
                                             Value="0"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <!-- Historia -->
                    <Border Grid.Column="1"
                            Style="{StaticResource OrangeCard}"
                            Margin="0,0,10,0"
                            Height="260"
                            Padding="12">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="Historia budżetu"
                                       Style="{StaticResource CardHeader}"
                                       Margin="0,0,0,6"/>

                            <TextBlock x:Name="BudgetHistoryChartHintText"
                                       Grid.Row="1"
                                       Text="Wybierz budżet, aby zobaczyć historię."
                                       Foreground="{DynamicResource App.Foreground}"
                                       Opacity="0.75"
                                       Margin="0,0,0,6"
                                       TextWrapping="Wrap"/>

                            <TextBlock x:Name="BudgetHistoryChartErrorText"
                                       Grid.Row="1"
                                       Text=""
                                       Foreground="Tomato"
                                       Opacity="0.95"
                                       Margin="0,0,0,6"
                                       Visibility="Collapsed"
                                       TextWrapping="Wrap"/>

                            <Border Grid.Row="2"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Padding="0">
                                <lvc:CartesianChart x:Name="BudgetHistoryChartControl"
                                                   Background="Transparent"
                                                   Foreground="{DynamicResource App.Foreground}"
                                                   LegendPosition="Hidden"
                                                   ZoomMode="None"
                                                   AnimationsSpeed="0:0:0.25" />
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Analiza -->
                    <Border Grid.Column="2"
                            Style="{StaticResource OrangeCard}"
                            Height="260"
                            Padding="12">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="Analiza budżetu"
                                       Style="{StaticResource CardHeader}"
                                       Margin="0,0,0,8"/>

                            <ItemsControl Grid.Row="1"
                                          x:Name="BudgetInsightsList">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource InsightItemBorder}">
                                            <TextBlock Text="{Binding}" Style="{StaticResource InsightItemText}"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>
                </Grid>

                <!-- RZĄD 2 -->
                <Grid Grid.Row="1" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="0.95*"/>
                    </Grid.ColumnDefinitions>

                    <!-- LEWA: lista budżetów -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <StackPanel Background="Transparent">

                            <TextBlock Text="Wybierz budżet, aby zobaczyć szczegóły"
                                       Foreground="{DynamicResource App.Foreground}"
                                       Opacity="0.9"
                                       Margin="0,0,0,10"/>

                            <Grid Margin="0,0,0,10" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="240"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox x:Name="TypeFilterCombo"
                                          Grid.Column="0"
                                          SelectedIndex="0"
                                          SelectedValuePath="Tag"
                                          SelectionChanged="TypeFilterCombo_SelectionChanged"/>

                                <!-- Używa globalnego SearchBoxStyle (tak jak w CategoriesPage) -->
                                <TextBox x:Name="SearchBox"
                                         Grid.Column="2"
                                         Style="{StaticResource SearchBoxStyle}"
                                         TextChanged="SearchBox_TextChanged"
                                         ToolTip="Wyszukiwanie po nazwie budżetu lub typie (np. Miesięczny, Inny)"/>
                            </Grid>

                            <Border Style="{StaticResource MiniTableHeaderBorder}">
                                <Grid Background="Transparent">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2.4*"/>
                                        <ColumnDefinition Width="1.1*"/>
                                        <ColumnDefinition Width="1.1*"/>
                                        <ColumnDefinition Width="1.1*"/>
                                        <ColumnDefinition Width="1.6*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="Budżet" Style="{StaticResource MiniTableHeaderText}"/>

                                    <TextBlock Grid.Column="1" Text="Zaplanowano" Style="{StaticResource MiniTableHeaderText}"
                                               HorizontalAlignment="Stretch" TextAlignment="Right"/>

                                    <TextBlock Grid.Column="2" Text="Wydano" Style="{StaticResource MiniTableHeaderText}"
                                               HorizontalAlignment="Stretch" TextAlignment="Right"/>

                                    <TextBlock Grid.Column="3" Text="Przekroczono" Style="{StaticResource MiniTableHeaderText}"
                                               HorizontalAlignment="Stretch" TextAlignment="Right"/>

                                    <TextBlock Grid.Column="4" Text="Okres" Style="{StaticResource MiniTableHeaderText}"/>
                                    <TextBlock Grid.Column="5" Text="" Style="{StaticResource MiniTableHeaderText}"/>
                                </Grid>
                            </Border>

                            <ListBox x:Name="BudgetsList"
                                     ItemContainerStyle="{StaticResource BudgetListItemStyle}"
                                     SelectionChanged="BudgetsList_SelectionChanged"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2,2" Background="Transparent">

                                            <Grid MinHeight="46" Background="Transparent">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2.4*"/>
                                                    <ColumnDefinition Width="1.1*"/>
                                                    <ColumnDefinition Width="1.1*"/>
                                                    <ColumnDefinition Width="1.1*"/>
                                                    <ColumnDefinition Width="1.6*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" Orientation="Vertical" Background="Transparent">
                                                    <TextBlock Text="{Binding Name}" Style="{StaticResource BudgetNameCell}"/>
                                                    <TextBlock Text="{Binding TypeDisplay}" Style="{StaticResource BudgetTypeSub}"/>
                                                </StackPanel>

                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding PlannedAmount, StringFormat={}{0:N2} zł}"
                                                           Style="{StaticResource AmountPlannedGreen}"/>

                                                <TextBlock Grid.Column="2"
                                                           Text="{Binding SpentAmount, StringFormat={}{0:N2} zł}"
                                                           Style="{StaticResource AmountSpentOrange}"/>

                                                <TextBlock Grid.Column="3"
                                                           Text="{Binding OverDisplayAmount, StringFormat={}{0:N2} zł}"
                                                           Style="{StaticResource AmountOverRed}"/>

                                                <TextBlock Grid.Column="4"
                                                           Text="{Binding Period}"
                                                           Style="{StaticResource MiniTableCellText}"/>

                                                <!-- PRZYCISKI 1:1 jak w CategoriesPage -->
                                                <StackPanel Grid.Column="5"
                                                            Orientation="Horizontal"
                                                            HorizontalAlignment="Right"
                                                            VerticalAlignment="Center"
                                                            Margin="0"
                                                            Background="Transparent">
                                                    <Button Style="{StaticResource EditIconButton}"
                                                            ToolTip="Edytuj"
                                                            Click="EditBudget_Click"
                                                            Tag="{Binding}"/>
                                                    <Button Style="{StaticResource DeleteIconButton}"
                                                            ToolTip="Usuń"
                                                            Click="DeleteBudget_Click"
                                                            Tag="{Binding}"/>
                                                </StackPanel>
                                            </Grid>

                                            <!-- Inline confirm: układ 1:1 jak w CategoriesPage -->
                                            <Border Margin="0,6,0,0"
                                                    HorizontalAlignment="Right"
                                                    Background="{DynamicResource Surface.Background}"
                                                    BorderBrush="{DynamicResource Surface.Border}"
                                                    BorderThickness="1"
                                                    CornerRadius="6"
                                                    Padding="8"
                                                    Visibility="{Binding IsDeleteConfirmVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                                <StackPanel>
                                                    <TextBlock Text="Czy na pewno chcesz usunąć budżet?"
                                                               Foreground="{DynamicResource App.Foreground}"
                                                               TextWrapping="Wrap"/>
                                                    <StackPanel Orientation="Horizontal"
                                                                HorizontalAlignment="Right"
                                                                Margin="0,6,0,0">
                                                        <Button Content="Tak"
                                                                Height="24"
                                                                MinWidth="40"
                                                                Padding="6,0"
                                                                Margin="0,0,6,0"
                                                                Click="DeleteBudgetConfirmYes_Click"
                                                                Tag="{Binding}"/>
                                                        <Button Content="Nie"
                                                                Height="24"
                                                                MinWidth="40"
                                                                Padding="6,0"
                                                                Click="DeleteBudgetConfirmNo_Click"
                                                                Tag="{Binding}"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>

                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Border Background="{DynamicResource Surface.Background}"
                                    BorderBrush="{DynamicResource Surface.Border}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="10"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="AddBudget_Click"
                                    Margin="0,10,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Background="Transparent">
                                    <TextBlock Text="+"
                                               FontSize="18"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource App.Foreground}"
                                               Margin="0,0,8,0"/>
                                    <TextBlock Text="Dodaj budżet"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource App.Foreground}"/>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="Transparent" BorderThickness="0"/>

                    <!-- PRAWA: transakcje budżetu -->
                    <Border Grid.Column="2" Style="{StaticResource Card}">
                        <Grid Background="Transparent">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="10"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Background="Transparent">
                                <TextBlock Text="Transakcje powiązane z budżetem"
                                           Style="{StaticResource CardHeader}"/>
                                <TextBlock x:Name="BudgetTransactionsHintText"
                                           Text="Wybierz budżet z listy po lewej, aby zobaczyć jego transakcje."
                                           Foreground="{DynamicResource App.Foreground}"
                                           Opacity="0.75"
                                           Margin="0,4,0,0"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="2"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled"
                                          PanningMode="VerticalOnly"
                                          Background="Transparent">
                                <ItemsControl x:Name="BudgetTransactionsList"
                                              BorderThickness="0"
                                              Background="Transparent"
                                              ItemTemplate="{StaticResource BudgetTransactionCardTemplate}"/>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                </Grid>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
