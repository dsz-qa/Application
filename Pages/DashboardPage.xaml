<UserControl x:Class="Finly.Pages.DashboardPage"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:controls="clr-namespace:Finly.Views.Controls"
 xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
 x:Name="Root">

 <UserControl.Resources>
 <Style x:Key="Card" TargetType="Border">
 <Setter Property="Background" Value="Transparent"/>
 <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border}"/>
 <Setter Property="BorderThickness" Value="1"/>
 <Setter Property="CornerRadius" Value="10"/>
 <Setter Property="Padding" Value="12"/>
 <Setter Property="Margin" Value="0,0,12,0"/>
 </Style>
 <Style x:Key="AccentCard" TargetType="Border" BasedOn="{StaticResource Card}">
 <Setter Property="BorderBrush" Value="{DynamicResource Brand.Blue}"/>
 <Setter Property="BorderThickness" Value="1.5"/>
 </Style>
 <Style x:Key="AccentOrangeCard" TargetType="Border" BasedOn="{StaticResource Card}">
 <Setter Property="BorderBrush" Value="{DynamicResource Brand.Orange}"/>
 <Setter Property="BorderThickness" Value="1.5"/>
 </Style>
 <!-- Zmieniono wygląd głównych nagłówków kafelków: większe, pogrubione, biała czcionka -->
 <Style x:Key="CardHeader" TargetType="TextBlock">
 <Setter Property="FontSize" Value="16"/>
 <Setter Property="FontWeight" Value="Bold"/>
 <Setter Property="Opacity" Value="1"/>
 <Setter Property="Foreground" Value="White"/>
 </Style>
 <Style x:Key="KpiValueStyle" TargetType="TextBlock">
 <Setter Property="FontSize" Value="18"/>
 <Setter Property="FontWeight" Value="Bold"/>
 <Setter Property="Margin" Value="0,2,0,0"/>
 <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
 </Style>
 <Style x:Key="KpiCard" TargetType="Border">
 <Setter Property="Background" Value="Transparent"/>
 <Setter Property="BorderBrush" Value="{DynamicResource Brand.Blue}"/>
 <Setter Property="BorderThickness" Value="2"/>
 <Setter Property="CornerRadius" Value="10"/>
 <Setter Property="Padding" Value="12"/>
 <Setter Property="Margin" Value="0,0,16,0"/>
 </Style>
 <Style TargetType="{x:Type DatePickerTextBox}">
 <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
 <Setter Property="Background" Value="{DynamicResource Surface.Background}"/>
 <Setter Property="BorderBrush" Value="{DynamicResource Brand.Blue}"/>
 <Setter Property="BorderThickness" Value="1.5"/>
 </Style>
 <!-- Styl nagłówków KPI (większe niż kwoty, pogrubione, białe) -->
 <Style x:Key="KpiHeader" TargetType="TextBlock">
 <Setter Property="FontSize" Value="20"/>
 <Setter Property="FontWeight" Value="Bold"/>
 <Setter Property="Foreground" Value="White"/>
 <Setter Property="Opacity" Value="1"/>
 </Style>
 </UserControl.Resources>

 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" PanningMode="VerticalOnly">
 <Grid Margin="16">
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto"/>
 <RowDefinition Height="Auto"/>
 <RowDefinition Height="*"/>
 </Grid.RowDefinitions>

 <!-- KPI row -->
 <Grid Grid.Row="0" Margin="0,0,0,12">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 </Grid.ColumnDefinitions>
 <Border Grid.Column="0" Style="{StaticResource KpiCard}"><StackPanel><TextBlock Text="Cały majątek" Style="{StaticResource KpiHeader}"/><TextBlock x:Name="TotalWealthText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/></StackPanel></Border>
 <Border Grid.Column="1" Style="{StaticResource KpiCard}"><StackPanel><TextBlock Text="Konta bankowe" Style="{StaticResource KpiHeader}"/><TextBlock x:Name="BanksText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/></StackPanel></Border>
 <Border Grid.Column="2" Style="{StaticResource KpiCard}"><StackPanel><TextBlock Text="Wolna gotówka" Style="{StaticResource KpiHeader}"/><TextBlock x:Name="FreeCashDashboardText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/></StackPanel></Border>
 <Border Grid.Column="3" Style="{StaticResource KpiCard}"><StackPanel><TextBlock Text="Odłożona gotówka (do rozdysponowania)" Style="{StaticResource KpiHeader}" TextWrapping="Wrap"/><TextBlock x:Name="SavedToAllocateText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/></StackPanel></Border>
 <Border Grid.Column="4" Style="{StaticResource KpiCard}"><StackPanel><TextBlock Text="Gotówka w kopertach" Style="{StaticResource KpiHeader}"/><TextBlock x:Name="EnvelopesDashboardText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/></StackPanel></Border>
 <Border Grid.Column="5" Style="{StaticResource KpiCard}"><StackPanel><TextBlock Text="Inwestycje" Style="{StaticResource KpiHeader}"/><TextBlock x:Name="InvestmentsText" Style="{StaticResource KpiValueStyle}" Text="0,00 zł"/></StackPanel></Border>
 </Grid>

 <!-- Insights/Alerts/Forecast row -->
 <Grid Grid.Row="1" Margin="0,0,0,12">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 </Grid.ColumnDefinitions>
 <Border Grid.Column="0" Style="{StaticResource AccentOrangeCard}" Padding="12">
 <StackPanel>
 <TextBlock Text="Analizy finansowe" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <ItemsControl ItemsSource="{Binding DataContext.Insights, ElementName=Root}">
 <ItemsControl.ItemTemplate>
 <DataTemplate><TextBlock Text="{Binding}" Foreground="{DynamicResource App.Foreground}" Margin="0,2,0,2" TextWrapping="Wrap"/></DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 </StackPanel>
 </Border>
 <Border Grid.Column="1" Style="{StaticResource AccentOrangeCard}" Padding="12">
 <StackPanel>
 <TextBlock Text="Alerty budżetowe" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <ItemsControl ItemsSource="{Binding DataContext.Alerts, ElementName=Root}">
 <ItemsControl.ItemTemplate>
 <DataTemplate><TextBlock Text="{Binding}" Foreground="{DynamicResource App.Foreground}" Margin="0,2,0,2" TextWrapping="Wrap"/></DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 </StackPanel>
 </Border>
 <Border Grid.Column="2" Style="{StaticResource AccentOrangeCard}" Padding="12">
 <StackPanel>
 <TextBlock Text="Prognoza końca miesiąca" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <Grid>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 </Grid.ColumnDefinitions>
 <StackPanel Grid.Column="0"><TextBlock Text="Przew. wydatki" Style="{StaticResource CardHeader}"/><TextBlock Text="{Binding DataContext.Forecast.PredictedSpending, ElementName=Root, StringFormat={}{0:N2} zł}" Foreground="{DynamicResource App.Foreground}" FontWeight="Bold"/></StackPanel>
 <StackPanel Grid.Column="1"><TextBlock Text="Przew. wolne środki" Style="{StaticResource CardHeader}"/><TextBlock Text="{Binding DataContext.Forecast.PredictedFreeCash, ElementName=Root, StringFormat={}{0:N2} zł}" Foreground="{DynamicResource App.Foreground}" FontWeight="Bold"/></StackPanel>
 </Grid>
 </StackPanel>
 </Border>
 </Grid>

 <!-- Main content aligned grid: period bar + two columns x three equal rows + tables row -->
 <Border Grid.Row="2" Style="{StaticResource AccentCard}" Padding="16" HorizontalAlignment="Stretch">
 <Grid>
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto"/>
 <RowDefinition Height="*"/>
 <RowDefinition Height="*"/>
 <RowDefinition Height="*"/>
 <RowDefinition Height="Auto"/>
 </Grid.RowDefinitions>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="*"/>
 </Grid.ColumnDefinitions>

 <!-- Period bar -->
 <controls:PeriodBarControl Grid.Row="0" Grid.ColumnSpan="2" x:Name="PeriodBar" HorizontalAlignment="Center" Margin="0,0,0,8" RangeChanged="PeriodBar_RangeChanged" SearchClicked="PeriodBar_SearchClicked" ClearClicked="PeriodBar_ClearClicked" />

 <!-- Row1: expenses donut / spacer (moved Top categories to Row3) -->
 <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,8,0">
 <StackPanel>
 <TextBlock Text="Wydatki wg kategorii — bieżący okres" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <lvc:PieChart Series="{Binding DataContext.ExpensesByCategorySeries, ElementName=Root}" Height="220" LegendPosition="Right" Background="Transparent" Foreground="{DynamicResource App.Foreground}" DrawMargin="0">
 <lvc:PieChart.LegendTextPaint><lvc:SolidColorPaint Color="#FFFFFFFF"/></lvc:PieChart.LegendTextPaint>
 </lvc:PieChart>
 </StackPanel>
 </Border>
 <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Card}" Margin="8,0,0,0" Background="Transparent">
 <!-- Spacer to keep equal height with left expenses donut -->
 </Border>

 <!-- Row2: income donut / planned transactions -->
 <Border Grid.Row="2" Grid.Column="0" Style="{StaticResource Card}" Margin="0,12,8,0">
 <StackPanel>
 <TextBlock Text="Źródła przychodu — bieżący okres" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <lvc:PieChart
 Series="{Binding DataContext.IncomeBySourceSeries, ElementName=Root}"
 Height="220"
 LegendPosition="Right"
 Background="Transparent"
 Foreground="{DynamicResource App.Foreground}"
 DrawMargin="0">
 <lvc:PieChart.LegendTextPaint>
 <lvc:SolidColorPaint Color="#FFFFFFFF"/>
 </lvc:PieChart.LegendTextPaint>
 </lvc:PieChart>
 </StackPanel>
 </Border>
 <Border Grid.Row="2" Grid.Column="1" Style="{StaticResource Card}" Margin="8,12,0,0">
 <StackPanel>
 <TextBlock Text="Zaplanowane transakcje" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <Border Margin="0,0,0,4" Background="{DynamicResource Surface.Background}" Padding="6,4,6,4">
 <Grid>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="140"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="90"/>
 </Grid.ColumnDefinitions>
 <TextBlock Grid.Column="0" Text="Data" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="1" Text="Kategoria" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="2" Text="Typ" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="3" Text="Konto" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="4" Text="Opis" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="5" Text="Kwota" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8" HorizontalAlignment="Right"/>
 </Grid>
 </Border>
 <ItemsControl x:Name="PlannedTransactionsList" ItemsSource="{Binding DataContext.PlannedTransactions, ElementName=Root}">
 <ItemsControl.ItemTemplate>
 <DataTemplate>
 <Grid Margin="0,2,0,2">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="140"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="90"/>
 </Grid.ColumnDefinitions>
 <TextBlock Grid.Column="0" Text="{Binding DateDisplay}" Foreground="{DynamicResource App.Foreground}"/>
 <TextBlock Grid.Column="1" Text="{Binding Category}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="2" Text="{Binding Kind}" Foreground="{DynamicResource App.Foreground}"/>
 <TextBlock Grid.Column="3" Text="{Binding Account}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="4" Text="{Binding Description}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="5" Text="{Binding AmountStr}" HorizontalAlignment="Right" Foreground="{DynamicResource App.Foreground}"/>
 </Grid>
 </DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 <TextBlock x:Name="PlannedEmptyText" Text="Brak zaplanowanych transakcji" Foreground="{DynamicResource App.Foreground}" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,8,0,0" FontStyle="Italic"/>
 </StackPanel>
 </Border>

 <!-- Row3: trend / Top categories (moved here) -->
 <Border Grid.Row="3" Grid.Column="0" Style="{StaticResource Card}" Margin="0,12,8,0" MinHeight="220">
 <StackPanel>
 <TextBlock Text="Trend wydatków — bieżący okres" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <Canvas x:Name="ExpenseTrendCanvas" Height="180" Margin="0,4,0,4" Background="Transparent"/>
 <ItemsControl x:Name="ExpenseTrendLabels" Margin="0,2,0,0" HorizontalAlignment="Stretch">
 <ItemsControl.ItemsPanel>
 <ItemsPanelTemplate><UniformGrid Rows="1"/></ItemsPanelTemplate>
 </ItemsControl.ItemsPanel>
 <ItemsControl.ItemTemplate>
 <DataTemplate><TextBlock Text="{Binding DateLabel}" FontSize="10" Foreground="{DynamicResource App.Foreground}" HorizontalAlignment="Center"/></DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 <TextBlock x:Name="ExpenseTrendEmptyText" Text="Brak danych w tym okresie" Foreground="{DynamicResource App.Foreground}" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,8,0,0" FontStyle="Italic"/>
 </StackPanel>
 </Border>
 <Border Grid.Row="3" Grid.Column="1" Style="{StaticResource Card}" Margin="8,12,0,0">
 <StackPanel>
 <TextBlock Text="Najwyższe wydatki — kategorie" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <ItemsControl x:Name="TopCategoryBars">
 <ItemsControl.ItemTemplate>
 <DataTemplate>
 <StackPanel Margin="0,4,0,6">
 <Grid>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="Auto"/>
 </Grid.ColumnDefinitions>
 <TextBlock Text="{Binding Name}" Foreground="{DynamicResource App.Foreground}" Margin="0,0,8,2" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="1" Text="{Binding PercentStr}" Foreground="{DynamicResource App.Foreground}"/>
 </Grid>
 <ProgressBar Minimum="0" Maximum="100" Height="10" Value="{Binding Percent}"/>
 </StackPanel>
 </DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 <TextBlock x:Name="TopCategoryEmptyText" Text="Brak danych w tym okresie" Foreground="{DynamicResource App.Foreground}" Visibility="Collapsed" HorizontalAlignment="Center" Margin="4,8,4,0" FontStyle="Italic"/>
 </StackPanel>
 </Border>

 <!-- Row4: tables (unchanged) -->
 <Border Grid.Row="4" Grid.Column="0" Style="{StaticResource Card}" Margin="0,12,8,0">
 <StackPanel>
 <TextBlock Text="Przychody — bieżący okres" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <Border Margin="0,0,0,4" Background="{DynamicResource Surface.Background}" Padding="6,4,6,4">
 <Grid>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="90"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="140"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="90"/>
 </Grid.ColumnDefinitions>
 <TextBlock Grid.Column="0" Text="Kwota" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8" HorizontalAlignment="Right"/>
 <TextBlock Grid.Column="1" Text="Kategoria" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="2" Text="Data" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="3" Text="Konto" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="4" Text="Opis" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="5" Text="Typ" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 </Grid>
 </Border>
 <ItemsControl x:Name="IncomeTable" ItemsSource="{Binding DataContext.Incomes, ElementName=Root}">
 <ItemsControl.ItemTemplate>
 <DataTemplate>
 <Grid Margin="0,2,0,2">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="90"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="140"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="90"/>
 </Grid.ColumnDefinitions>
 <TextBlock Grid.Column="0" Text="{Binding AmountStr}" HorizontalAlignment="Right" Foreground="{DynamicResource App.Foreground}"/>
 <TextBlock Grid.Column="1" Text="{Binding Category}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="2" Text="{Binding DateDisplay}" Foreground="{DynamicResource App.Foreground}"/>
 <TextBlock Grid.Column="3" Text="{Binding Account}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="4" Text="{Binding Description}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="5" Text="{Binding Kind}" Foreground="{DynamicResource App.Foreground}"/>
 </Grid>
 </DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 <TextBlock x:Name="IncomeEmptyText" Text="Brak danych w tym okresie" Foreground="{DynamicResource App.Foreground}" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,8,0,0" FontStyle="Italic"/>
 </StackPanel>
 </Border>

 <Border Grid.Row="4" Grid.Column="1" Style="{StaticResource Card}" Margin="8,12,0,0">
 <StackPanel>
 <TextBlock Text="Wydatki — bieżący okres" Style="{StaticResource CardHeader}" Margin="0,0,0,8"/>
 <Border Margin="0,0,0,4" Background="{DynamicResource Surface.Background}" Padding="6,4,6,4">
 <Grid>
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="90"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="140"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="90"/>
 </Grid.ColumnDefinitions>
 <TextBlock Grid.Column="0" Text="Kwota" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8" HorizontalAlignment="Right"/>
 <TextBlock Grid.Column="1" Text="Kategoria" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="2" Text="Data" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="3" Text="Konto" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="4" Text="Opis" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 <TextBlock Grid.Column="5" Text="Typ" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource App.Foreground}" Opacity="0.8"/>
 </Grid>
 </Border>
 <ItemsControl x:Name="ExpenseTable" ItemsSource="{Binding DataContext.Expenses, ElementName=Root}">
 <ItemsControl.ItemTemplate>
 <DataTemplate>
 <Grid Margin="0,2,0,2">
 <Grid.ColumnDefinitions>
 <ColumnDefinition Width="90"/>
 <ColumnDefinition Width="*"/>
 <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="140"/>
 <ColumnDefinition Width="2*"/>
 <ColumnDefinition Width="90"/>
 </Grid.ColumnDefinitions>
 <TextBlock Grid.Column="0" Text="{Binding AmountStr}" HorizontalAlignment="Right" Foreground="{DynamicResource App.Foreground}"/>
 <TextBlock Grid.Column="1" Text="{Binding Category}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="2" Text="{Binding DateDisplay}" Foreground="{DynamicResource App.Foreground}"/>
 <TextBlock Grid.Column="3" Text="{Binding Account}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="4" Text="{Binding Description}" Foreground="{DynamicResource App.Foreground}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Grid.Column="5" Text="{Binding Kind}" Foreground="{DynamicResource App.Foreground}"/>
 </Grid>
 </DataTemplate>
 </ItemsControl.ItemTemplate>
 </ItemsControl>
 <TextBlock x:Name="ExpenseEmptyText" Text="Brak danych w tym okresie" Foreground="{DynamicResource App.Foreground}" Visibility="Collapsed" HorizontalAlignment="Center" Margin="0,8,0,0" FontStyle="Italic"/>
 </StackPanel>
 </Border>

 </Grid>
 </Border>

 </Grid>
 </ScrollViewer>
</UserControl>
