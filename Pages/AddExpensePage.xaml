<UserControl x:Class="Finly.Pages.AddExpensePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Karta / panel -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background"      Value="{DynamicResource Surface.Background}"/>
            <Setter Property="BorderBrush"     Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius"    Value="10"/>
            <Setter Property="Padding"         Value="16"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <TextBlock Grid.Row="0"
                   Text="Dodaj"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{DynamicResource App.Foreground}"/>

        <!-- DUŻE ZAKŁADKI NA ŚRODKU -->
        <TabControl Grid.Row="1"
                    Width="900"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,24,0,0"
                    Background="Transparent"
                    BorderBrush="{DynamicResource Surface.Border}"
                    BorderThickness="1"
                    Padding="0">

            <!-- Wygląd samych zakładek -->
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="40,10"/>
                    <Setter Property="FontSize" Value="16"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Margin" Value="4,0"/>
                    <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
                </Style>
            </TabControl.Resources>

            <!-- ================= WYDATEK ================= -->
            <TabItem Header="Wydatek">
                <Border Style="{StaticResource Card}" Margin="0,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Kwota:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox   x:Name="ExpenseAmountBox"
                                   Grid.Row="0" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Kategoria:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox  x:Name="ExpenseCategoryBox"
                                   Grid.Row="2" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="4" Grid.Column="0"
                                   Text="Data:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="ExpenseDatePicker"
                                    Grid.Row="4" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="6" Grid.Column="0"
                                   Text="Opis:" VerticalAlignment="Top" Margin="0,4,8,0"/>
                        <TextBox   x:Name="ExpenseDescBox"
                                   Grid.Row="6" Grid.Column="1"
                                   Height="80"
                                   TextWrapping="Wrap"
                                   AcceptsReturn="True"
                                   VerticalScrollBarVisibility="Auto"/>

                        <!-- Przyciski -->
                        <StackPanel Grid.Row="8" Grid.ColumnSpan="2"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Content="Dodaj"
                                    Margin="0,0,8,0"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="SaveExpense_Click"/>
                            <Button Content="Anuluj"
                                    Click="Cancel_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ================= PRZYCHÓD ================= -->
            <TabItem Header="Przychód">
                <Border Style="{StaticResource Card}" Margin="0,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Kwota:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox   x:Name="IncomeAmountBox"
                                   Grid.Row="0" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Źródło / kategoria:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox   x:Name="IncomeSourceBox"
                                   Grid.Row="2" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="4" Grid.Column="0"
                                   Text="Data:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="IncomeDatePicker"
                                    Grid.Row="4" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="6" Grid.Column="0"
                                   Text="Opis:" VerticalAlignment="Top" Margin="0,4,8,0"/>
                        <TextBox   x:Name="IncomeDescBox"
                                   Grid.Row="6" Grid.Column="1"
                                   Height="80"
                                   TextWrapping="Wrap"
                                   AcceptsReturn="True"
                                   VerticalScrollBarVisibility="Auto"/>

                        <StackPanel Grid.Row="8" Grid.ColumnSpan="2"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Content="Dodaj"
                                    Margin="0,0,8,0"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="SaveIncome_Click"/>
                            <Button Content="Anuluj"
                                    Click="Cancel_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ================= TRANSFER ================= -->
            <TabItem Header="Transfer">
                <Border Style="{StaticResource Card}" Margin="0,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Z konta:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox  x:Name="TransferFromBox"
                                   Grid.Row="0" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Na konto:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox  x:Name="TransferToBox"
                                   Grid.Row="2" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="4" Grid.Column="0"
                                   Text="Kwota:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox   x:Name="TransferAmountBox"
                                   Grid.Row="4" Grid.Column="1" Height="28"/>

                        <TextBlock Grid.Row="6" Grid.Column="0"
                                   Text="Data:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <DatePicker x:Name="TransferDatePicker"
                                    Grid.Row="6" Grid.Column="1" Height="28"/>

                        <StackPanel Grid.Row="8" Grid.ColumnSpan="2"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Content="Zapisz transfer"
                                    Margin="0,0,8,0"
                                    Style="{StaticResource PrimaryButton}"
                                    Click="SaveTransfer_Click"/>
                            <Button Content="Anuluj"
                                    Click="Cancel_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>








