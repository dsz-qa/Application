<UserControl x:Class="Finly.Pages.LoansPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Grid Margin="16">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
			<TextBlock Text="Kredyty" FontSize="20" FontWeight="Bold"
                       Foreground="{DynamicResource App.Foreground}"/>

			<Button Content="Dodaj kredyt" Margin="12,0,0,0" Padding="8,4" Click="AddLoan_Click"/>
		</StackPanel>

		<Border Grid.Row="1" Margin="0,12,0,0"
                Background="{DynamicResource Surface.Background}"
                BorderBrush="{DynamicResource Surface.Border}" BorderThickness="1"
                CornerRadius="10" Padding="12">
			<StackPanel>
				<ItemsControl Name="LoansList">
					<ItemsControl.ItemTemplate>
						<DataTemplate>
							<Border Margin="4" Padding="8" Background="{DynamicResource Surface.Background}" CornerRadius="6">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="*" />
										<ColumnDefinition Width="Auto" />
									</Grid.ColumnDefinitions>

									<StackPanel>
										<TextBlock Text="{Binding Name}" FontWeight="Bold"/>
										<TextBlock Text="{Binding PrincipalStr}" Foreground="Gray"/>
									</StackPanel>

									<StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
										<Button Content="Usuń" Tag="{Binding}" Margin="4,0,0,0" Click="DeleteLoan_Click"/>
									</StackPanel>
								</Grid>
							</Border>
						</DataTemplate>
					</ItemsControl.ItemTemplate>
				</ItemsControl>

				<TextBlock Text="Brak kredytów." Foreground="Gray" Name="EmptyText" Visibility="Collapsed"/>
			</StackPanel>
		</Border>
	</Grid>
</UserControl>

