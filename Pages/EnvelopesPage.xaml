<UserControl x:Class="Finly.Pages.EnvelopesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Finly.Pages"
             xmlns:data="clr-namespace:System.Data;assembly=System.Data"
             mc:Ignorable="d"
             Background="{DynamicResource App.Background}"
             Foreground="{DynamicResource App.Foreground}"
             SnapsToDevicePixels="True"
             UseLayoutRounding="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType">

    <UserControl.Resources>

        <!-- Style wspólne -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface.Background}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Surface.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,12,16,0"/>
        </Style>

        <Style x:Key="AccentCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="BorderBrush" Value="{DynamicResource Brand.Blue}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="0,16,16,0"/>
        </Style>

        <Style x:Key="CardHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>

        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
        </Style>

        <!-- ===== KAFEL KOPERTY ===== -->
        <DataTemplate DataType="{x:Type data:DataRowView}">
            <Border Style="{StaticResource Card}" Width="340">
                <StackPanel>

                    <!-- Nagłówek -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6" Background="Transparent">
                        <Rectangle Width="24"
                                   Height="24"
                                   Margin="0,0,8,0"
                                   VerticalAlignment="Top"
                                   Fill="{DynamicResource Brand.Orange}">
                            <Rectangle.OpacityMask>
                                <ImageBrush Stretch="Uniform" ImageSource="/Assets/Icons/koperty.png"/>
                            </Rectangle.OpacityMask>
                        </Rectangle>

                        <TextBlock FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource App.Foreground}"
                                   TextWrapping="Wrap"
                                   MaxWidth="280"
                                   VerticalAlignment="Center">
                            <Run Text="{Binding Row[Name], Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Docelowa kwota -->
                    <TextBlock Text="Docelowa kwota"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[Target], StringFormat={}{0:N2} zł}"
                               Margin="0,2,0,6"
                               Foreground="{DynamicResource App.Foreground}"/>

                    <!-- Przydzielona kwota -->
                    <TextBlock Text="Przydzielona kwota"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[Allocated], StringFormat={}{0:N2} zł}"
                               Margin="0,2,0,6"
                               Foreground="{DynamicResource App.Foreground}"/>

                    <!-- Do celu pozostało -->
                    <TextBlock Text="Do celu pozostało"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[Remaining], StringFormat={}{0:N2} zł}"
                               Margin="0,2,0,6"
                               Foreground="{DynamicResource App.Foreground}"/>

                    <!-- Miesięcznie trzeba odkładać -->
                    <TextBlock Text="Miesięcznie musisz odkładać"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[MonthlyRequired], StringFormat={}{0:N2} zł}"
                               Margin="0,2,0,6"
                               Foreground="{DynamicResource App.Foreground}"/>

                    <!-- Termin -->
                    <TextBlock Text="Termin"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[Deadline]}"
                               Margin="0,2,0,6"
                               Foreground="{DynamicResource App.Foreground}"
                               TextWrapping="Wrap"/>

                    <!-- Cel (tekst) -->
                    <TextBlock Text="Cel"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[GoalText]}"
                               Margin="0,2,0,6"
                               Foreground="{DynamicResource App.Foreground}"
                               TextWrapping="Wrap"/>

                    <!-- Opis -->
                    <TextBlock Text="Opis"
                               FontWeight="Bold"
                               Foreground="{DynamicResource App.Foreground}"
                               Opacity="0.9"/>
                    <TextBlock Text="{Binding Row[Description]}"
                               Margin="0,2,0,4"
                               Foreground="{DynamicResource App.Foreground}"
                               TextWrapping="Wrap"/>

                    <!-- PRZYCISKI -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,10,0,0">
                        <Button Margin="0,0,8,0"
                                Tag="{Binding}"
                                Click="EditEnvelope_Click"
                                Style="{StaticResource EditIconButton}"
                                ToolTip="Edytuj kopertę"/>
                        <Button Tag="{Binding}"
                                Click="DeleteEnvelope_Click"
                                Style="{StaticResource DeleteIconButton}"
                                ToolTip="Usuń kopertę"/>
                    </StackPanel>

                    <!-- PANEL POTWIERDZENIA USUNIĘCIA -->
                    <StackPanel x:Name="DeleteConfirmPanel"
                                Orientation="Vertical"
                                Margin="0,6,0,0"
                                Visibility="Collapsed">
                        <TextBlock Text="Czy na pewno chcesz usunąć kopertę?"
                                   FontSize="12"
                                   Foreground="{DynamicResource App.Foreground}"
                                   Margin="0,0,0,4"
                                   TextWrapping="Wrap"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="Tak"
                                    Padding="8,3"
                                    MinWidth="60"
                                    Tag="{Binding}"
                                    Click="ConfirmDeleteEnvelope_Click"
                                    Margin="0,0,8,0"/>
                            <Button Content="Nie"
                                    Padding="8,3"
                                    MinWidth="60"
                                    Click="CancelDeleteEnvelope_Click"/>
                        </StackPanel>
                    </StackPanel>

                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- ===== KAFEL „DODAJ KOPERTĘ” ===== -->
        <DataTemplate DataType="{x:Type local:AddEnvelopeTile}">
            <Border Style="{StaticResource Card}"
                    Width="260"
                    Cursor="Hand"
                    MouseLeftButtonUp="AddEnvelopeCard_Click">
                <Grid>
                    <Border Margin="10"
                            BorderThickness="1"
                            CornerRadius="8"
                            BorderBrush="{DynamicResource Surface.Border}"/>
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="0,16,0,16">
                        <TextBlock Text="+"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource App.Foreground}"/>
                        <TextBlock Text="Dodaj kopertę"
                                   Margin="0,6,0,0"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource App.Foreground}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

    </UserControl.Resources>

    <!-- CAŁA STRONA -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24,24,24,60">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- KPI -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource AccentCard}">
                    <StackPanel>
                        <TextBlock Text="Gotówka w kopertach"
                                   Style="{StaticResource CardHeader}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="0,0,0,4"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="Suma środków przydzielonych do wszystkich kopert."
                                   Margin="0,2,0,0"
                                   Foreground="{DynamicResource App.Foreground}"
                                   Opacity="0.7"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="TotalEnvelopesText"
                                   Style="{StaticResource KpiValueStyle}"
                                   FontSize="24"
                                   Text="0,00 zł"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource AccentCard}">
                    <StackPanel>
                        <TextBlock Text="Wolna gotówka"
                                   Style="{StaticResource CardHeader}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="0,0,0,4"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="SavedCashText"
                                   Style="{StaticResource KpiValueStyle}"
                                   FontSize="24"
                                   Text="0,00 zł"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource AccentCard}">
                    <StackPanel>
                        <TextBlock Text="Odłożona gotówka (do rozdysponowania)"
                                   Style="{StaticResource CardHeader}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Margin="0,0,0,4"
                                   TextWrapping="Wrap"/>
                        <TextBlock x:Name="EnvelopesSumText"
                                   Style="{StaticResource KpiValueStyle}"
                                   FontSize="24"
                                   Text="0,00 zł"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Lista kopert -->
            <StackPanel Grid.Row="1" Margin="0,12,0,0">
                <Separator Height="1"
                           Background="{DynamicResource Surface.Border}"
                           Opacity="0.8"
                           Margin="0,0,0,8"/>

                <TextBlock Text="Koperty"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource App.Foreground}"
                           Margin="0,4,0,12"/>

                <ItemsControl x:Name="EnvelopesCards">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <TextBlock x:Name="UnassignedText"
                           Text="Brak przypisanej gotówki"
                           Visibility="Collapsed"
                           Margin="0,10,0,0"
                           Opacity="0.75"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>
